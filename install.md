# ``

``` text

1. libRepOutRecord.so
[~]$ sudo ln -sf /usr/local/inforsecclient/libRepOutRecord.so /usr/lib/libRepOutRecord.so
[~]$ sudo ln -sf /usr/local/inforsecclient/libRepOutRecord.so /usr/lib/libRepOutRecord.so.1


2.
```